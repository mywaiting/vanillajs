

type Context = {}

export function treeWalker(treeNode: Node, ctx: Context): ChildNode | null | void {
    const type = treeNode.nodeType
    // Element
    if (type === 1) {
    

    // Text
    } else if (type === 3) {
    
    
    // childNode
    } else if (type === 11) {
        
    }

}